# SPDX-License-Identifier: GPL-2.0-only
#
# agent-linux (AirymaxOS) memory submodule Kbuild
# 构建入口：MemoryRovol L1-L4 + CXL + PMEM + MGLRU 内核模块
# 决策 F1：最小可编译骨架（非桩）
#
# 构建命令：
#   make -C /lib/modules/$(uname -r)/build M=$(pwd) modules

obj-m := memoryrovol/
